name: guentravel
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Gestion d'état, requêtes HTTP et injection de dépendances
  flutter_bloc: ^8.1.3
  dio: ^5.4.0
  get_it: ^7.6.0
  intl: ^0.20.3
  equatable: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
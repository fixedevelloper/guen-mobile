<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 9+ (API 28+) bloque le trafic HTTP en clair par défaut. Le backend
  com.guentours tourne en HTTP en développement (émulateur/localhost) ; cette
  config n'autorise le clair que pour les hôtes de dev. Le domaine de
  production (guenstravel.com) doit toujours être servi en HTTPS et n'a pas
  besoin d'exception ici.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
